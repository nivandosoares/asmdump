# Track 1 Seed Window `0086..0093`

- Seed savestate: `.mesen-config/Mesen2/SaveStates/game_11.mss`
- Capture harness:
  - `validation/mesen_capture.lua`
  - `validation/mesen_dump_bg_range.lua`
  - `validation/mesen_probe_boot.lua`
- Input schedule:
  - hold `b` from script frames `60..359`

## Raw Window

- Flat dump summary:
  - `tools/out/track1_seed_0086_0093_v2.json`
- Runtime-ready sequence:
  - `tools/out/track1_seed_0086_0093_v2_sequence.txt`
  - `tools/out/track1_seed_0086_0093_v2_sequence.json`
- Captured raw frames:
  - `86..93`
  - each frame carries `vram.bin`, `cgram.bin`, `ppu_state.json`, and `oam.bin`

## Alignment

- Screenshot harness output `td2_track1_accel_frame_00026.png` matches the
  raw-dump render of frame `86` at `0` mismatched pixels.
- This confirms the savestate-backed flat dump and the screenshot harness refer
  to the same visible scene once the `b`-hold window becomes nontrivial.

## Current Reading

- The extracted scene is static across frames `86..93`:
  - `VRAM`, `CGRAM`, and `OAM` are unchanged between frames `86` and `93`
  - rendered BG output is unchanged between frames `86` and `93`
- Render summary for frames `86` and `93`:
  - `bgMode = 1`
  - `mainScreenLayers = 0x04`
- Probe-side callback/state context over the same window remains trivial:
  - `active_main = 00:8029`
  - `active_nmi = 00:8029`
  - tracked `$0202/$0204/$0206/$0208/$020A/$040A/$0054` stay `0`

## Practical Reading

- `game_11.mss` is a valid deterministic seed for a track-start visual state.
- This specific `86..93` raw window remains static under the original `b`-hold
  schedule.
- A later seeded sweep (`track1_seed_sweep_v1`) now shows that the same
  savestate does leave the static scene under bounded scripted inputs, but the
  earliest moving raw-dump follow-up is still blocked by a capture-surface
  mismatch.
