# Post-`1093` `00:8029` Window (`1102..1117`)

This note closes the next late-attract continuation read after the active
`1094..1117` behavior trace.

Primary artifacts:

- `tools/out/visual_contract_range_1102_1109_activity/visual_contract_range.json`
- `tools/out/visual_contract_range_1110_1117_activity/visual_contract_range.json`
- `tools/out/post_1093_compare_1102_1117/summary.json`
- `tools/out/post_1093_compare_1102_1117/summary.md`
- `tools/out/mode7_plateau_1105/analysis.json`
- `tools/out/mode7_plateau_1105/analysis.md`
- `tools/out/mode7_first_pixel_1105/audit.json`
- `tools/out/mode7_first_pixel_1105/audit.md`
- `tools/out/mode7_plateau_1117/analysis.json`
- `tools/out/mode7_plateau_1117/analysis.md`
- `tools/out/mode7_first_pixel_1117/audit.json`
- `tools/out/mode7_first_pixel_1117/audit.md`
- `tools/out/post_1093_compare_1102_1117_linebias1/summary.json`
- `tools/out/post_1093_compare_1102_1117_linebias1/summary.md`
- `tools/out/mode7_plateau_1105_linebias1/analysis.json`
- `tools/out/mode7_plateau_1105_linebias1/analysis.md`
- `tools/out/post_1093_compare_1102_1117_default/summary.json`
- `tools/out/post_1093_compare_1102_1117_default/summary.md`
- `tools/out/mode7_plateau_1105_default/analysis.json`
- `tools/out/mode7_plateau_1105_default/analysis.md`
- `rom_analysis/docs/mode7_1105_validation_reference.md`
- `rom_analysis/docs/mode7_plateau_first_pixel_audit.md`

Builder path:

- `python3 tools/build_mesen_visual_contract_range.py tools/out/design_mesen_range_1102_1109_v1 tools/out/visual_contract_range_1102_1109_activity --provenance-json rom_analysis/maps/tilemaps/mesen_range_1102_1109_provenance.jsonc --probe-json tools/out/activity_trace_1094_1117/td2_boot_probe.json --activity-trace-json tools/out/activity_trace_1094_1117/activity_trace.json --clean-out`
- `python3 tools/build_mesen_visual_contract_range.py tools/out/design_mesen_range_1110_1117_v1 tools/out/visual_contract_range_1110_1117_activity --provenance-json rom_analysis/maps/tilemaps/mesen_range_1110_1117_provenance.jsonc --probe-json tools/out/activity_trace_1094_1117/td2_boot_probe.json --activity-trace-json tools/out/activity_trace_1094_1117/activity_trace.json --clean-out`
- `python3 tools/build_mesen_window_compare.py tools/out/post_1093_compare_1102_1117/summary.json tools/out/mesen_range_1102_1109_v1 tools/out/mesen_range_1110_1117_v1 --activity-trace-json tools/out/activity_trace_1094_1117/activity_trace.json --markdown-out tools/out/post_1093_compare_1102_1117/summary.md`
- `python3 tools/build_mode7_plateau_analysis.py tools/out/post_1093_compare_1102_1117/summary.json tools/out/design_mesen_range_1102_1109_v1 tools/out/design_mesen_range_1110_1117_v1 tools/out/mode7_plateau_1105/analysis.json --markdown-out tools/out/mode7_plateau_1105/analysis.md`
- `python3 tools/build_mode7_first_pixel_audit.py tools/out/mode7_plateau_1105/analysis.json tools/out/mode7_first_pixel_1105/audit.json --markdown-out tools/out/mode7_first_pixel_1105/audit.md`
- `python3 tools/build_mode7_plateau_analysis.py tools/out/post_1093_compare_1102_1117/summary.json tools/out/design_mesen_range_1102_1109_v1 tools/out/design_mesen_range_1110_1117_v1 tools/out/mode7_plateau_1117/analysis.json --canonical-frame 1117 --markdown-out tools/out/mode7_plateau_1117/analysis.md`
- `python3 tools/build_mode7_first_pixel_audit.py tools/out/mode7_plateau_1117/analysis.json tools/out/mode7_first_pixel_1117/audit.json --markdown-out tools/out/mode7_first_pixel_1117/audit.md`
- `python3 tools/build_mesen_window_compare.py tools/out/post_1093_compare_1102_1117_linebias1/summary.json tools/out/mesen_range_1102_1109_v1 tools/out/mesen_range_1110_1117_v1 --activity-trace-json tools/out/activity_trace_1094_1117/activity_trace.json --mode7-line-bias 1 --markdown-out tools/out/post_1093_compare_1102_1117_linebias1/summary.md`
- `python3 tools/build_mode7_plateau_analysis.py tools/out/post_1093_compare_1102_1117_linebias1/summary.json tools/out/design_mesen_range_1102_1109_v1 tools/out/design_mesen_range_1110_1117_v1 tools/out/mode7_plateau_1105_linebias1/analysis.json --canonical-frame 1105 --mode7-line-bias 1 --markdown-out tools/out/mode7_plateau_1105_linebias1/analysis.md`
- `python3 tools/build_mesen_window_compare.py tools/out/post_1093_compare_1102_1117_default/summary.json tools/out/mesen_range_1102_1109_v1 tools/out/mesen_range_1110_1117_v1 --activity-trace-json tools/out/activity_trace_1094_1117/activity_trace.json --markdown-out tools/out/post_1093_compare_1102_1117_default/summary.md`
- `python3 tools/build_mode7_plateau_analysis.py tools/out/post_1093_compare_1102_1117_default/summary.json tools/out/design_mesen_range_1102_1109_v1 tools/out/design_mesen_range_1110_1117_v1 tools/out/mode7_plateau_1105_default/analysis.json --canonical-frame 1105 --markdown-out tools/out/mode7_plateau_1105_default/analysis.md`

## Window Shape

- sampled frames:
  - `1102..1117`
- the visible presentation surface stays flat across the whole block:
  - `bgMode = 7`
  - main-screen layer: `bg1`
  - visible OBJ count: `61`
- callback/state continuity also stays flat:
  - main callback: `00:8029`
  - IRQ callback: `00:835F`
  - `$0204 = 1`
  - `$0206 = 13`
  - `$040A = 17`
  - `$0054 = 128`
- contiguous tilemap provenance still stays under the same later carry family:
  - `1102..1117` -> `07:C112`

Reading:

- `1102` is a callback-family handoff, not a visible-surface handoff
- the continuation after `1102` is still one `bg1`/`61`-sprite presentation
  surface

## Activity Split

The merged `activityTrace` summaries now make the late producer boundary
explicit inside the same `00:8029` callback family:

- `1102..1113`
  - `1` DMA event per frame
  - DMA domain: `oam`
  - direct `VRAM/CGRAM` events: `0`
  - `Mode 7` events: `3`
  - `Mode 7` writes: `16`
- `1114..1117`
  - DMA events: `0`
  - direct `VRAM/CGRAM` events: `0`
  - `Mode 7` events: `3`
  - `Mode 7` writes: `16`

Reading:

- the `1114` boundary is real, but it is specifically an `OAM DMA` shutdown
- it is not a callback change
- it is not a layer change
- it is not a visible-sprite-count change
- it is not a new `Mode 7` programming shape

## Export Surface

The compare summary extends the earlier `1094..1101` export read across the
whole `00:8029` continuation:

- for every frame in this block:
  - `main.ppm` is `256x239`
  - `main_visible.ppm` is `256x224`
- exact compare result:
  - top `224` lines of `main.ppm` vs `main_visible.ppm` -> `0` mismatched
    pixels for all `16` frames
  - bottom `224` lines of `main.ppm` vs `main_visible.ppm` ->
    `14901..21300` mismatched pixels

Reading:

- `main_visible.ppm` is still the visible top crop of `main.ppm`
- the export-surface explanation from `1094..1101` survives through `1117`

## Mode 7 State Swap

The compare summary also narrows where visible-state substitution still matters.

Base render vs `main_visible.ppm`:

- `1102`: `838`
- `1103`: `1061`
- `1104`: `1798`
- `1105..1117`: `2698`

Visible-state render vs `main_visible.ppm`:

- `1102`: `6082`
- `1103`: `5958`
- `1104`: `6292`
- `1105..1117`: `2698`

The matrix deltas collapse by `1105`:

- `1102`: base `matrix[0]/[3] = 640/514`, visible `256/257`
- `1103`: base `512/514`, visible `256/257`
- `1104`: base `384/257`, visible `256/257`
- `1105..1117`: base and visible both `256/257`

Reading:

- visible-state substitution is still actively wrong for `1102..1104`
- that specific disagreement disappears by `1105`
- after `1105`, the remaining renderer gap is a flat `2698`-pixel plateau
  that no longer tracks end-of-frame vs visible-state `Mode 7` coefficients
- the updated compare artifact now proves that plateau is identical in shape
  and content across the whole tail:
  - same diff-mask hash from `1105..1117`
  - same diff payload hash from `1105..1117`
  - same bounding box `x=24..232`, `y=68..138`

## Official Python Scanline-Start Compare Path

The renderer-side `screenY + 1` hypothesis is now promoted into the default
Python compare path. `render_mesen_snes_bg.py`,
`build_mesen_window_compare.py`, and
`build_mode7_plateau_analysis.py` now default to `mode7LineBias = 1`, while
`--mode7-line-bias 0` stays available only as an explicit counterfactual.

Base render vs `main_visible.ppm` with `--mode7-line-bias 1`:

- `1102`: `0`
- `1103`: `0`
- `1104`: `0`
- `1105..1117`: `0`

Visible-state render vs `main_visible.ppm` with `--mode7-line-bias 1`:

- `1102`: `6045`
- `1103`: `5951`
- `1104`: `6066`
- `1105..1117`: `0`

Reading:

- promoting the scanline-start rule into the compare path closes the whole
  composed-screen window `1102..1117`
- the pre-plateau `1102..1104` frames are now best read as:
  - composed-screen issue solved under end-of-frame state plus `line + 1`
  - visible-state substitution still wrong
- by `1105`, both base-state and visible-state compares collapse to `0`
- the new no-flag default artifacts reproduce the earlier explicit
  `--mode7-line-bias 1` read modulo metadata-only path/timestamp differences

## Promoted Canonical `1105` Read

The updated plateau analyzer now accepts the zero-diff composed-screen case and
keeps the BG-only read alive under the same `--mode7-line-bias 1` path.

Canonical `1105` with `--mode7-line-bias 1`:

- `mode7-ppu` with OAM vs `main_visible.ppm`: `0`
- `simple` with OAM vs `main_visible.ppm`: `0`
- no-`OAM` vs `main_visible.ppm`: `7019`
- no-`OAM` vs `bg1_visible.ppm`: `2271`
- composed-screen diff bbox: `none`
- fallback sampling bbox for the BG-only read: `24,67 -> 231,120`

BG-only spot checks with the same bias:

- `1102`: `6032`
- `1103`: `5966`
- `1104`: `6176`
- `1105`: `2271`
- `1117`: `2271`

Reading:

- the composed-screen fix is no longer hypothetical; it is now reproducible in
  the regular compare path
- the isolated `bg1_visible` problem remains separate and survives even after
  the composed-screen window is closed
- the next frontier is no longer "which scanline-start term closes the screen?"
  but:
  - what still explains the BG-only/export mismatch after the official
    tooling rule is fixed at `line + 1`
  - and when the same rule can be promoted into the SDL runtime after the
    dirty `port/src/td2_ppu.c` worktree is resolved

## Practical Reading

The late continuation is now narrow enough to stop treating several boundaries
as likely causes:

- `1102` changes callback family, but not the visible surface
- `1105` removes the remaining visible-state `Mode 7` disagreement
- `1114` removes per-frame `OAM DMA`, but does not worsen the compare result

So the best current frontier is not:

- hidden direct `VRAM/CGRAM` uploads
- another callback fork
- a later `OAM` ownership change
- or a bare visible-state `Mode 7` substitution

The best current frontier is no longer the old `2698`-pixel composed-screen
plateau. That surface is now closed in the promoted compare path.

More sharply:

- the composed-screen rule is now narrow enough to describe explicitly:
  - end-of-frame state
  - `Mode 7` scanline term `screenY + 1`
- the remaining unresolved piece is the separate BG-only/export mismatch
  concentrated inside `24,67 -> 231,120`

## Shared Runtime Check

The same `line + 1` rule is now also promoted into the shared SDL runtime
despite the file being dirty from another process, because the active diff in
`port/src/td2_ppu.c` does not overlap the `render_snes_mode7_scene()` origin-Y
block.

Important validation caveat:

- `td2_port` auto-loads the default intro sequence unless given an explicit
  `--sequence`
- for isolated `--snes-bg-*` checks, use `--sequence /dev/null` so the default
  manifest does not overwrite the requested scene

Bounded runtime validation:

- `SDL_VIDEODRIVER=dummy ./port/build/td2_port --sequence /dev/null --headless --frames 1 --dump-prefix ./port/build/mode7_1102 --snes-bg-vram ./tools/out/mesen_range_1102_1109_v1/frame_01102/vram.bin --snes-bg-cgram ./tools/out/mesen_range_1102_1109_v1/frame_01102/cgram.bin --snes-bg-state ./tools/out/mesen_range_1102_1109_v1/frame_01102/ppu_state.json --snes-bg-oam ./tools/out/mesen_range_1102_1109_v1/frame_01102/oam.bin`
- `SDL_VIDEODRIVER=dummy ./port/build/td2_port --sequence /dev/null --headless --frames 1 --dump-prefix ./port/build/mode7_1105 --snes-bg-vram ./tools/out/mesen_range_1102_1109_v1/frame_01105/vram.bin --snes-bg-cgram ./tools/out/mesen_range_1102_1109_v1/frame_01105/cgram.bin --snes-bg-state ./tools/out/mesen_range_1102_1109_v1/frame_01105/ppu_state.json --snes-bg-oam ./tools/out/mesen_range_1102_1109_v1/frame_01105/oam.bin`
- `SDL_VIDEODRIVER=dummy ./port/build/td2_port --sequence /dev/null --headless --frames 1 --dump-prefix ./port/build/mode7_1117 --snes-bg-vram ./tools/out/mesen_range_1110_1117_v1/frame_01117/vram.bin --snes-bg-cgram ./tools/out/mesen_range_1110_1117_v1/frame_01117/cgram.bin --snes-bg-state ./tools/out/mesen_range_1110_1117_v1/frame_01117/ppu_state.json --snes-bg-oam ./tools/out/mesen_range_1110_1117_v1/frame_01117/oam.bin`

Runtime compare results vs Mesen `main_visible.ppm`:

- `1102`: `0`
- `1105`: `0`
- `1117`: `0`

Control compare from a temporary `HEAD` rebuild of `td2_ppu.c`:

- `1102`: `838`
- `1105`: `2698`

Reading:

- the runtime path now agrees with the official Python/tooling rule on the
  composed-screen slice
- the worktree conflict was a boundary-management problem, not a semantic
  blocker in the `Mode 7` line-origin code

## Canonical Static Plateau

The updated compare artifact now also fingerprints the key extracted inputs.

From `1105..1117`:

- `main_visible.ppm` is byte-identical
- `vram.bin` is byte-identical
- `oam.bin` is byte-identical
- `ppu_state.json` is byte-identical
- `cgram.bin` still changes every frame

Reading:

- the plateau can now be treated as one canonical scene state from `1105`
  onward for geometry/layout purposes
- the remaining per-frame `CGRAM` churn is not changing the visible image in
  this tail

Canonical `1105` object sanity check:

- current `mode7-ppu` render vs `main_visible.ppm`: `2698` mismatched pixels
- `simple` OBJ render vs `main_visible.ppm`: `2698`
- no-`OAM` render vs `main_visible.ppm`: `9717`

Reading:

- visible objects are required for the scene
- but the remaining fixed plateau is not explained by a difference between the
  current `simple` and `mode7-ppu` object compositor paths
- the usual late-PPU suspects are not active in the canonical state:
  - `ppu.colorMathEnabled = 0`
  - `ppu.subScreenLayers = 0`
  - window masks are inactive
- the next likely frontier is fixed BG/composition/color-math semantics inside
  the same static scene

## Canonical BG-Only Narrowing

The new plateau analyzer promotes the earlier manual `1105` checks into one
reproducible artifact.

Plateau identity:

- the same trailing canonical plateau auto-resolves to `1105..1117`
- `bg1_visible.ppm` is also byte-identical across that whole `13`-frame tail

Canonical `1105` BG-only compare:

- no-`OAM` render vs `bg1_visible.ppm`: `3982` mismatched pixels
- the BG-only diff box is still the same plateau family:
  - `24,67 -> 232,138`
  - this is only `1` row taller than the main plateau box `24,68 -> 232,138`

Sprite overlap against the main plateau diff box:

- only `4` visible sprites touch that box at all
- combined sprite coverage inside the box is `128 / 14839` pixels
  (`0.862592%`)

BG horizontal-shift scan inside the BG-only diff box:

- zero-shift mismatch: `3982`
- best whole-box shift: `-1`
- best-shift mismatch: `3611`
- per-row best-shift counts:
  - `-1`: `38` rows
  - `-8`: `16`
  - `0`: `13`
  - `-2`: `4`
  - `-3`: `1`

Direct `Mode 7` `hscroll` scan on the same canonical state:

- base `ppu.mode7.hscroll = 0`
- BG-only compare:
  - best tested delta is `+1`
  - mismatch improves `3982 -> 3613`
- full-scene compare:
  - best tested delta is still the unmodified state `0`
  - naive `hscroll +1` worsens `2698 -> 2780`

Reading:

- the canonical plateau is overwhelmingly BG-driven, not sprite-driven
- a small horizontal shift helps, but it does not solve the whole box
- a direct `hscroll +1` bias reproduces most of that BG-only gain
- but because it makes the full-scene compare worse, the missing rule is not a
  trivial global `hscroll` offset
- so the remaining frontier is not "missing sprites" and not a pure global
  translation either
- the updated sampling stats now also prove the plateau bbox never leaves the
  `1024x1024` Mode 7 map under either the base state or the best tested
  `hscroll +1` variant:
  - `outsideMapPixels = 0`
  - base BG bbox sample range: `x = 24..232`, `y = 65..137`
  - `hscroll +1` BG bbox sample range: `x = 25..233`, `y = 65..137`
- so `M7SEL` outside-map fill/transparent behavior is no longer a leading
  explanation for the plateau
- the best current read is now a fixed `Mode 7/BG1` horizontal sampling,
  first-pixel/X-origin, or visible-latched scroll rule inside the static
  plateau

## Exact-Hit First-Pixel Audit

The next direct audit of the static plateau tested only exact-hit models taken
from the narrowed source read:

- base current renderer
- visible-state current renderer
- increment-before-sample
- `pixel 0` / X-origin `+1`
- scanline `Y + 1`

Results at both plateau endpoints (`1105` and `1117`) were identical:

- base current:
  - full scene `2698`
  - BG-only `3982`
- visible-state current:
  - full scene `2698`
  - BG-only `3982`
- increment-before-sample:
  - full scene `2780`
  - BG-only `3613`
- X-origin `+1`:
  - full scene `2780`
  - BG-only `3613`
- scanline `Y + 1`:
  - full scene `0`
  - BG-only `2271`

Reading:

- focused visible-state `Mode 7` deltas are absent on the static plateau
- `increment-before-sample` and `X-origin +1` collapse to the same result on
  this state because:
  - `M7A = 256`
  - `M7C = 0`
- the first model that actually closes the composed scene is the scanline
  start term `screenY + 1`
- that means the leading plateau frontier is no longer a horizontal/X-origin
  correction alone; it is the `Mode 7` scanline-start rule used by the
  composed-screen renderer
- the remaining isolated `bg1_visible` diff after that change is now a second
  question, not proof that the composed-screen fix is wrong
